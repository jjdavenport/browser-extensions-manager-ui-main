const Nav = () => {
  return (
    <>
      <section>
        <h2>Extensions List</h2>
        <nav>
          <button>All</button>
          <button>Active</button>
          <button>Inactive</button>
        </nav>
      </section>
    </>
  );
};

export default Nav;
